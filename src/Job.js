class Job {
    constructor (newCompany, newRole, newDateRange) {
        this.company = newCompany
        this.role = newRole
        this.dateRange = newDateRange
    }
    
    toString () {
        return `<div><span>`
    }
}